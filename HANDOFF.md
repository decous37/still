# Still 前端交接说明

更新：2026-09-10。本文描述当前透光视觉与轻量交互版本，覆盖早先日志中的列表直出和提示规则。原 logs 保留，不新增日志。

## 本次布局修复

- 顶栏第二行使用统一容器：可用宽度不足48rem时三按钮整体横排居中，不足19rem时统一等宽纵排；宽屏保持练习/偏好居中、集合靠右。
- 删除主按钮旧的36px顶部外边距。组末操作宽屏两列等宽同高，窄屏单列；实际检查宽屏两按钮均为232×48px且顶部相同，375px下均为312×48px。
- 集合改为标准模态Sheet，触发按钮没有高层级。抽屉全尺寸从顶部展开，遮罩为50、抽屉为51；删除顶栏高度测量及手机偏移。标题/关闭/模块切换固定，列表独立滚动。
- 本轮浏览器检查：320、375、390、600、767、768、864、865、1024、1440px无横向溢出；窄屏按钮横排或统一纵排，点击高度44px。中英明暗代表性页面通过视觉检查。
- 英文宽屏及中文手机的三个模块按钮通过中心命中检查，无外部Collection遮挡；手机抽屉顶部为0。Escape、内部关闭、遮罩关闭及焦点回归通过；Shift+Tab保持在抽屉内。短窗口1024×500检查通过。
- 类型检查、22项现有测试通过；生产构建通过。未完成200%实际浏览器缩放、全尺寸与语言主题的完整笛卡尔组合及真实读屏测试，不把这些项目标作通过。
- 本次未修改题库、分组存储或声音；未新增日志、提交或发布。

## 当前行为

- 首页直接进入重组第一组第一题；当前600题，中英文各300题，各模块十组、每组十题，保留原204题ID。
- 顶栏固定保留练习、偏好，集合是独立带框按钮。抽屉默认关闭；通过内部关闭按钮、遮罩、Escape关闭，外部顶栏在模态期间不可操作。
- 抽屉和偏好页阻止输入和自动换题，关闭恢复此前状态。选择当前题保留输入，选择其他题或模式重置。后台等待恢复不会被抽屉关闭解除。
- 句子和重组显示固定底稿，按 answers[0] 从左向右填绿。相同文本可用任意尚未使用的对应字块。备用答案数据保留，但不用于本次底稿匹配。
- 错误整条文本变红并轻晃，300ms 后整题重置；反馈期间锁定输入，候选顺序不变。没有可见提示、错误次数或成功文案。
- 正确输入一次45ms高通键击，错误一次90ms柔和单滴；音效共用静音开关，触感单独控制。取消反馈会停止活动音源并使待恢复音频请求失效。
- 整题成功停留250ms，过渡160ms后自动换题。正确完成按题目ID记录一次，本地次数仍存于 still:question-completions:v1；集合显示次数数字，未完成为0。
- 阅读补回仍先读后隐藏；错误重置保留缺词状态，不揭示答案。统一清理句末句号、问号、叹号和省略号，保留闭引号及括号，题面和集合引用同一展示内容。
- 语言切换映射相同题号并重置输入，主题切换保留输入。后台暂停保留所有输入，回来显示“点击继续”，题面大点击区支持键盘恢复；过渡被后台打断后重新显示完整成功停留。
- 底部撤回、暂停、结束入口已删除。组内第十题后显示组末页，提供下一组/再练本组，最后一组改为选择题组。
- 进度带关联单组去重完成数，按语言在本机独立保存；切模式和语言保留。再练本组只清当前组进度，不清历史次数。
- 细框透光按钮、静态蓝粉金外围光晕、明暗同源玻璃；仅集合抽屉背景模糊，候选按键外层不位移。进度带有原生progress读屏语义，减少动态效果时不动画。

## 文件地图

| 文件 | 当前职责 |
| --- | --- |
| app/page.tsx → components/still/air-app.tsx | 页面入口、偏好、完成页、本地进度与集合协调 |
| components/still/collection-drawer.tsx | Sheet 抽屉、Tabs 模式选择、完成次数和选题 |
| components/still/live-practice.tsx | 输入状态、暂停、定时反馈及自动换题 |
| components/still/draft-text.tsx | 固定底稿、绿色填充、整条错误反馈、阅读缺词 |
| components/still/candidate-words.tsx | 独立候选块与已使用占位 |
| components/still/engine.ts | 与可见底稿一致的判断、错误锁定和 retry 状态 |
| components/still/questions.ts | 204题；保留ID、备用答案，统一清理补回展示句末标点 |
| components/still/feedback.ts | 音效与触感降级 |
| app/globals.css → air.css → live.css → fill.css | 按序加载；fill.css为本次视觉与抽屉覆盖层 |
| components/still/engine.test.ts、feedback.test.ts | 题库、暂停恢复、错误锁定及音源取消测试 |

旧六题页面组件已在此前版本删除。当前仍为本地网站，无账户、后端、埋点或线上部署。

## 开发与验证

在 /Users/decous/TempFiles/Earn_tring/still 使用现有npm锁文件和React/Vinext栈。

```sh
npm run dev
npx tsc --noEmit
npx vitest run
npm run build
```

测试覆盖：204题结构、句号处理、重复文字、底稿顺序与备用答案的区别、错误阶段锁定、retry清空、暂停阻止retry、阅读错误后继续隐藏。

本轮浏览器检查：独立导航、集合默认隐藏、完成次数显示、部分输入打开集合后保留、无底部操作，以及中英明暗代表性布局。检查了手机、768px平板与1280px桌面；768px无横向溢出。此前版本验证过错误重置、阅读隐藏和末题结束，本轮未重新完整跑遍这些浏览器流程。

自动化覆盖新增68道补回展示一致性、闭引号内外句末标点、后台恢复成功停留、一次音源触发、取消待播放/活动声音、音频不可用降级。交付前运行类型检查、相关文件lint、15项测试与生产构建。

未作完整验证：Safari/手机实际听感与震动、200%放大、完整读屏流程、减少动态效果实机体验、后台切换与快速连点全流程，以及玻璃模糊开关的性能录制对照。CSS和状态实现不等同于已实测；这些项目仍需后续验收。

鼠标指针停住再跳跃的根因仍未确定。本次只避免候选命中区位移并限制模糊范围，不声明已修复。继续参考 docs/MOUSE_DIAGNOSTIC.md。

## 后续边界

维持固定底稿与250/300/160ms反馈约定。不要恢复大标题、可见提示或列表直出，不增加新的业务系统。保留题目ID与用户本地完成记录。内容第二人复核、真人试用及注意力效果研究仍未完成。

# Still 前端交接说明

## 新增本地实验：幻彩

已新增独立懒加载的幻彩原型：中英各12题、连续随机练习、120 BPM合成音乐、局内计分与连击、暂停与结算。配置默认关闭；按最新要求将源码和文档提交至GitHub，并以 `VITE_PRISM_ENABLED=true` 构建更新现有公开站点。后续重新发布时需显式保留该构建变量，否则入口关闭。详细架构、验收与限制见 [幻彩交接](./docs/PRISM.md)。下文保留普通模式的交接记录。

更新：2026-09-17。当前为600题长度重写、双空补回及轻键版本；此前日志仅供历史参考。

## 轻键 / Key Flow

- `key-flow.ts` 定义固定19键顺序、事件过滤与偏好读写；`use-key-flow.ts` 负责设备默认值和监听生命周期。独立键 `still:key-flow` 保存布尔字符串，已有选择优先，否则使用 `(hover: hover) and (pointer: fine)`；存储禁用时退回会话。
- 候选数组顺序映射 `asdfghjklqwertyuiop`，不按剩余选项重排；按实际字母识别，兼容大小写。第20项起没有映射，保留点击和Tab操作。
- 所有选择共用原 `pick` 入口。入口读取最新状态防止快速输入穿透锁定；已用候选无操作、无音效。按住重复、组合键、输入法组合和可编辑目标被过滤。
- 仅练习可操作且补回已隐藏时注册选词监听。集合、偏好、后台暂停和成功／错误／过渡阶段不接收字母；关闭功能移除监听与键帽，不清空输入。Tab、Enter、空格不拦截。
- 候选内部右上角显示键帽及无障碍快捷键说明；点击和按键均短暂按压内部视觉层，减少动态效果时不位移。题库、完成记录和声音未更改。
- 按用户后续要求，本次更新提交至GitHub并发布至现有Sites站点，保持公开访问权限。

### 本次验证

- 类型检查、24项自动化测试和生产构建通过。新增测试覆盖19键顺序、大小写、过滤条件、偏好存储降级，以及三模式沿用固定候选与原判定。
- 浏览器实测中文窄屏键位提示、正确选词、重复按已用键无变化、集合内按键不作答、Escape回归、关闭轻键后隐藏提示且保留输入。
- 未实测完整中英文×明暗×桌面／手机矩阵、200%缩放、真实输入法／读屏、硬件长按与后台恢复端到端流程；不能以纯函数测试替代这些验证。
- 当前Sites构建包装脚本可用，已通过；下方保留上一轮验证记录。

## 题库

- 中英文各300题；每种语言三个模式，每模式十组，每组十题。
- 每组第1题短、第9题长，其余八题中等。全库60短、480中、60长。
- 中文重组分别3–5、6–7、8–10字；英文重组分别4–6、7–9、10–12字母。
- 中文句子与补回分别10–15、16–24、25–32字；英文分别6–8、9–13、14–18词。
- 句子词块分别3–4、5–6、7–8块。中文排除标点与空格，英文缩写和连字符词计一词。
- 原600题ID、主题归属和组内位置保留，内容已经改写。英文重组小写，句子按正常语法大写。
- 全部内容集中在 `components/still/group-content.ts`；每行使用显式ID，不再引用旧题正文。格式为 `id~内容`，句子用 `|` 分块，补回以方括号标记空位，末尾追加两个干扰词。
- `questions.ts` 定义 `LengthTier`、长度标准和 `RecallSegment`。补回的 `segments` 是有序文本／空位结构，原句、隐藏和完成题面同源；不再使用单空 `before/after`。
- 判定严格使用 `answers[0]`，重复字符按文字及剩余数量接受。

## 补回与反馈

- 补回作答阶段空格切换隐藏／显示，独立于轻键开关，复用 `reveal` 清空本次输入。忽略长按、输入法组合及修饰键；非题面控件保留原生空格行为。暂停、集合、偏好和反馈过渡期间不监听。

- 短题一空三选一；中长题两空，两个不同答案与两个干扰词共用四个固定候选。
- 先读再隐藏；两空同时隐藏，从左到右填写。第一空填绿，已使用候选禁用并占位。
- 任意空位错误后整句闪红300ms，再重置所有空位及候选，保持隐藏，不揭示答案。
- “再读一遍”清空本次作答并显示原句；再次隐藏从第一空开始。
- 第一空只播放输入音；整题完成播放单次金属轻响，完成次数只累计一次。错误仍为单滴音。
- 保留250ms成功停留、160ms过渡。集合、偏好、后台暂停继续冻结输入和定时任务；后台返回仍需点击继续。
- 空位使用固定宽度，隐藏时不在题面DOM或读屏标签中放入答案。当前空位有下划线与读屏位置提示；全句红色覆盖已填绿内容。
- 候选允许换行，命中区域不随悬停位移。保留现有字号、主题、减少动态效果和抽屉行为。

## 存储版本

- 完成次数：`still:question-completions:v2`。
- 组进度：`still:group-progress:v2`。
- v2不读取v1记录，不从旧次数初始化进度。旧的两个v1记录键和v1迁移标记完全不写入、不删除，原样作为本机备份保留。
- 刷新保留v2记录；首页仍从重组第一组第一题开始。“再练本组”只清当前语言当前组的v2进度，不清次数。
- 语言、主题、声音和触感偏好键不变。存储异常退回会话状态。
- 完成次数读取过滤负数、非数字和非整数，避免损坏的本机数据影响显示。

## 验证结果

- 类型检查、20项测试、差异空白检查通过。测试遍历全部600题，覆盖长度档位、词块数、完整分组、原ID与位置、重复文本、大小写规则、答案可完成性。
- 原始分组保存为 `group-membership.fixture.json` 测试基准；不要随题库变化自动更新它来掩盖ID或顺序变动。
- 状态测试覆盖重复字符、锁定、暂停恢复、双空顺序、第二空错误、错误后保持隐藏、再次阅读重置；存储测试覆盖v1保留、v2归零、刷新读取、重练和不可用降级。
- 浏览器实际检查：中文双空先读后隐藏、第一空填绿与禁用、第二空错误后两空重置、键盘Enter作答、集合开关保留部分输入、Escape关闭、整题完成通知。
- 390px手机视口的中文明暗题面已检查，无横向溢出，候选命中区域大于44px。临时视口设置已恢复。
- 未完整实测：所有最长题在中英文×明暗×手机／桌面的组合、200%放大、真实屏幕阅读器、Safari及手机音效和触感、后台与快速连点端到端回归。读屏位置通知已检查DOM，但不等于真实读屏测试。
- Sites构建包装脚本在当前安装中缺失，改用项目原有 `npm run build`，生产构建通过。
- 鼠标漂移根因仍未确认，参考 [诊断记录](./docs/MOUSE_DIAGNOSTIC.md)，不声称此次更新解决该问题。

## 维护边界

本轮无新依赖、后端、计时、排名或自适应难度，未新增日志。中英文README与题库审核说明同步更新；按用户后续要求使用中文提交信息提交并推送GitHub，不发布网站。

内容经过本轮作者自查，尚无独立第二人复核和真人试用。加长题目不代表已证明能改善真实工作或阅读中的注意力。

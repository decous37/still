# Still 前端交接说明

更新：2026-09-09。本文供接手模型阅读，先核对代码再修改，不需要重新设计页面。

## 1. 本轮边界与产品约定

用户最新要求：删除六题小练习及其旧数据，默认进入“整理一句话”，练习集只保留三种模式入口；练习页上方提供当前练习集题目列表，支持选题，并在每题右侧显示本地持久化的完成次数。

产品是低刺激的中英文文字练习。首页直接进入“整理一句话”，不增加登录、开始确认、积分、排行榜、倒计时、自动加难或后台统计。模式入口放在练习集，当前只有三种完整模式入口，每种语言每种模式 34 题。不要宣称已证明能恢复注意力。

## 2. 运行与入口

项目目录：`/Users/decous/TempFiles/Earn_tring/still`。

```sh
npm run dev
npx tsc --noEmit
npx vitest run
npm run build
```

使用现有 npm 锁文件。React 19、TypeScript、Vinext、Tailwind 与 Vitest 已安装；保留现有技术栈。不要另建第二套前端。已有服务器时复用终端报告的地址，通常为 `http://localhost:3000`。

活动入口：`app/page.tsx` → `components/still/air-app.tsx` → `live-practice.tsx`。

页面通过 hash 切换：`#practice`、`#library`、`#settings`、`#complete`，不是四条服务端路由。不需要引入后端才能继续工作。

## 3. 文件地图与已完成内容

| 文件                                                  | 职责                                                                                |
| ----------------------------------------------------- | ----------------------------------------------------------------------------------- |
| `components/still/air-app.tsx`                        | 顶栏、练习集、偏好、结束页、页面导航、模式入口和本地完成次数持久化                  |
| `components/still/live-practice.tsx`                  | 当前练习集题目列表、题面、输入位置、字块、暂停画面、选题、状态协调和换题定时器      |
| `components/still/engine.ts`                          | 已有本地判定状态更新：输入、错误、撤回、暂停等                                      |
| `components/still/questions.ts`                       | 中英文各三类、每类 34 题，共 204 题；组卷与答案前缀辅助函数                         |
| `components/still/feedback.ts`                        | Web Audio 短键击声、设备震动和反馈停止                                              |
| `components/still/use-feedback-preferences.ts`        | 本机声音、触感偏好                                                                  |
| `components/still/use-preferences.ts`                 | 语言、明暗主题                                                                      |
| `app/layout.tsx`                                      | 页面元信息、首绘前主题和语言设置                                                    |
| `app/globals.css` → `app/air.css` → `app/live.css`    | 按此顺序加载；后两层承载新界面和轻盈交互样式                                        |
| `components/still/engine.test.ts`、`vitest.config.ts` | 七个测试与独立测试配置，已覆盖 204 题结构、完整模式练习集、句子去句号和核心输入边界 |
| `docs/CONTENT_AUDIT.md`                               | 题库扩写范围、结构审核状态和题目 ID 清单                                            |
| `docs/MOUSE_DIAGNOSTIC.md`                            | 鼠标卡顿与漂移的已知事实、样式整理和后续实测矩阵                                    |

旧 `still-app.tsx`、`start-scene.tsx`、`practice-scene.tsx`、`samples.ts`、`copy.ts`、`top-bar.tsx`、`complete-scene.tsx`、`text-block.tsx`、`block-answer-body.tsx`、`recall-body.tsx` 已删除，避免旧六题样例数据继续误导接手。

本轮已移除当前题库生成中固定“每模式 12 题”的实现假设和六题练习数据：题目现在来自显式内容集合，旧题 ID `0` 至 `11` 保持稳定，新增题 ID 为 `12` 至 `33`。网页不再显示“完整混合题库”入口；练习集页面只显示三种模式入口，进入后练习页上方显示当前模式全部 34 题的列表。请继续把内容复核视为必要交付项，未经真人或第二人复核的文案不要写成完全验收。

前端已具备：直接练习、三种题面、中英切换、明暗切换、顶栏静音、声音与震动开关、正确输入反馈、局部红字与轻晃、轻提示、暂停画面、练习集和结束页。样式包含冰蓝背景层次、薄按键质感、已使用占位、移动端换行和减少动态效果适配。

## 4. 接手模型的业务接入约定

保持组件可见结构和 CSS 类名稳定，优先审查已有实现、补测试，再提取状态协调；避免业务改动附带视觉重写。

- 当前模式类型为 `word | sentence | recall`，语言为 `en | zh-CN`。题目保存独立块 ID、展示文本、可接受文本序列；阅读题额外保存缺词前后文。
- 错误输入不消耗字块、不推进位置；当前输入会立即清空并回到输入态，同时在开启触感时触发一次短震动。
- 相同文字按可用数量处理；不要把两个相同字块的 ID 当成正确性差异。
- 排序使用所有可接受答案的前缀判定，不能因唯一预设顺序误伤其他自然语序。现有替代答案覆盖有限，需继续人工审核题目。
- 正确输入触发一次短音效；错误只触发可选 20ms 震动，不播放正常输入声音，也不显示需要用户处理的继续提示。任何设备 API 失败都不能阻止练习。
- 完成后保留 450ms，再用 160ms 过渡换题；这段时间禁止再次输入。不要增加“提交”或“继续”门槛。
- 暂停、离页、后台切换和语言切换需要取消挂起的换题及反馈。恢复后不能跳题或重复触发。
- 切换语言保留当前模式，重置当前输入，并选中该语言当前模式的第一题；切换主题保留输入。
- 首页默认进入 `sentence` 模式，即“整理一句话”。练习集可切换 `word | sentence | recall`，每个模式都是当前语言下完整 34 题。题目列表可直接选题，完成次数按题目 ID 存入 `localStorage` 的 `still:question-completions:v1`。
- 不擅自增加账户、API、数据库、云同步、埋点或远程内容生成。

需要重点审查：语言切换与完成定时器的竞态、后台暂停的清理、结束后通过顶栏返回练习的行为、快速双击、提示与撤回后的错误次数、阅读重新显示原句时的输入状态、声音异步初始化与静音的先后顺序。本文仅列接入与审核要求，不附新增业务实现。

## 5. 验证状态与未完成验证

已验证：TypeScript 检查和生产构建；七个单元测试通过，覆盖 204 题基本结构、完整模式练习集 34 题、句子去句号、重复文字、替代语序、错误后清空重输、暂停锁定及阅读隐藏前禁输。`npx oxlint` 对本轮相关文件通过；全仓 `npm run lint` 仍会被既有 `components/ui` 与 `hooks/use-mobile.ts` 规则问题阻断，未在本轮处理。

此前浏览器实际检查了中英文、明暗主题、375px 手机宽度、768px 平板宽度及 1440px 桌面宽度；手机布局测得无横向溢出。已观察到错误标红、二次错误提示、重复汉字修正、自动换题、静音开关状态、离页暂停及阅读补回流程。本轮改为题目列表与本地完成次数后，仍需做完整浏览器回归。

尚不能据此宣称全部验收通过：Safari/真实手机的音频与震动、真实听感、200% 文字放大、完整读屏操作、后台切换竞态、最后一题自然结束后的全部返回路径仍需复验。此前开发过程出现过热更新 hook 错误，完整刷新后恢复；若再次出现，应检查正式加载而非仅看热更新状态。

鼠标问题：本轮只处理了有代码证据的样式重叠风险。`app/air.css` 的全局字块 hover 不再负责位移，`app/live.css` 的字块上浮只对精细指针、可用且未使用的字块生效。用户描述的是指针本身停住再跳跃，因此仍需按 `docs/MOUSE_DIAGNOSTIC.md` 做内嵌预览、独立浏览器、开发服务、生产预览和静态页面对照；无法复现时不要写“已修复”。

小规模真人试用尚未进行。请评估声音是否疲劳、错误是否紧张、450ms 是否过快；不要虚构试用结论或注意力改善数据。

交付仍为本地预览。`.openai/hosting.json` 尚无项目 ID，未发布，不要声称已有线上站点。

## 6. 可直接发给接手模型的任务

> 先阅读 HANDOFF.md、docs/CONTENT_AUDIT.md、docs/MOUSE_DIAGNOSTIC.md 和当前活动入口。保留现有视觉、页面和交互约定，审查 engine.ts、questions.ts 与 live-practice.tsx 中已有的初版逻辑，优先补足竞态和边界测试，再修复问题。不要增加登录、数据库或统计功能，不要改回开始选择页。完成后报告实际测试结果和仍需真机验证的项目，不宣称已证明注意力训练效果。

# Still 前端交接说明

更新：2026-09-09。本文描述当前填句改版，覆盖早先日志中的列表直出和提示规则。原 logs 保留，不新增日志。

## 当前行为

- 首页直接进入句子练习；204 题与所有题目 ID 保留，中英文各 102 题，每种模式 34 题。
- 顶栏“集合”默认关闭，侧边抽屉统一选模式和题目；同一按钮、关闭按钮、遮罩、Escape 均可关闭。手机抽屉位于顶栏下方，页面不被推挤。
- 抽屉打开阻止输入和自动换题，关闭恢复此前状态；手动暂停不会被关闭抽屉操作解除。选择当前题保留输入，选择其他题或模式重置。
- 句子和重组显示固定底稿，按 answers[0] 从左向右填绿。相同文本可用任意尚未使用的对应字块。备用答案数据保留，但不用于本次底稿匹配。
- 错误整条文本变红并轻晃，300ms 后整题重置；反馈期间锁定输入，候选顺序不变。没有可见提示、错误次数或成功文案。
- 正确输入一次短音，整题成功停留250ms，过渡160ms后自动换题。正确完成按题目ID记录一次，本地次数仍存于 still:question-completions:v1；集合仅显示完成标记。
- 阅读补回仍先读后隐藏；错误重置保留缺词状态，不揭示答案。
- 语言切换映射相同题号并重置输入，主题切换保留输入。后台切换保留手动暂停，需主动恢复。
- 页面移除大标题、口号和常驻列表。仅集合抽屉使用小范围背景模糊，候选按键外层不位移。

## 文件地图

| 文件 | 当前职责 |
| --- | --- |
| app/page.tsx → components/still/air-app.tsx | 页面入口、偏好、完成页、本地进度与集合协调 |
| components/still/collection-drawer.tsx | Sheet 抽屉、Tabs 模式选择、完成标记和选题 |
| components/still/live-practice.tsx | 输入状态、暂停、定时反馈及自动换题 |
| components/still/draft-text.tsx | 固定底稿、绿色填充、整条错误反馈、阅读缺词 |
| components/still/candidate-words.tsx | 独立候选块与已使用占位 |
| components/still/engine.ts | 与可见底稿一致的判断、错误锁定和 retry 状态 |
| components/still/questions.ts | 204题；未修改ID、备用答案或内容 |
| components/still/feedback.ts | 音效与触感降级 |
| app/globals.css → air.css → live.css → fill.css | 按序加载；fill.css为本次视觉与抽屉覆盖层 |
| components/still/engine.test.ts | 9个题库与状态测试 |

旧六题页面组件已在此前版本删除。当前仍为本地网站，无账户、后端、埋点或线上部署。

## 开发与验证

在 /Users/decous/TempFiles/Earn_tring/still 使用现有npm锁文件和React/Vinext栈。

```sh
npm run dev
npx tsc --noEmit
npx vitest run
npm run build
```

测试覆盖：204题结构、句号处理、重复文字、底稿顺序与备用答案的区别、错误阶段锁定、retry清空、暂停阻止retry、阅读错误后继续隐藏。

浏览器已检查：默认隐藏集合、输入填绿、错误锁定与重置、打开关闭集合后保留输入、手动暂停后关闭集合仍暂停、Escape关闭及焦点回归、切换模式、阅读隐藏流程、中英明暗布局、末题自然结束。375px视口无横向溢出，游戏文字为30px。最终类型检查、相关文件lint、9个测试与生产构建均通过。

未作完整真机验证：Safari/手机实际声音与震动、200%放大、完整读屏流程、减少动态效果实机体验，以及玻璃模糊开关的性能录制对照。CSS包含相关适配，但不要把实现等同于已实测。

鼠标指针停住再跳跃的根因仍未确定。本次只避免候选命中区位移并限制模糊范围，不声明已修复。继续参考 docs/MOUSE_DIAGNOSTIC.md。

## 后续边界

维持固定底稿与250/300/160ms反馈约定。不要恢复大标题、可见提示或列表直出，不增加新的业务系统。保留题目ID与用户本地完成记录。内容第二人复核、真人试用及注意力效果研究仍未完成。
